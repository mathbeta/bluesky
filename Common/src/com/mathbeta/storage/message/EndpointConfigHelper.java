package com.mathbeta.storage.message;

import java.net.ServerSocket;
import java.net.Socket;

public class EndpointConfigHelper {
	public static void config(Socket socket, EndpointConfig config) {
		
	}
	
	public static void config(ServerSocket server, EndpointConfig config) {
		
	}
}
